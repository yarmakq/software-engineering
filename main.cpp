#include <stdio.h>
#include <iostream>

using namespace std;

int main() {
    int a, b;
    cout << "Enter A: ";

    cin >> a;
    cout << "Enter B: ";
    cin >> b;
    cout << "A + B = " << a + b << '\n';
    cout << "A - B = " << a - b << '\n';
    cout << "A / B = " << a / b << '\n';
}
